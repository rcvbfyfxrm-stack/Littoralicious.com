{
  "name": "littoralicious",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Littoralicious publication — single source of truth (data/articles.json) + one-command publish.",
  "scripts": {
    "new": "node tools/new.mjs",
    "build": "node tools/build.mjs",
    "check": "node tools/build.mjs --check",
    "validate": "node tools/validate.mjs",
    "lint": "node tools/lint.mjs",
    "rewrite": "node tools/rewrite.mjs",
    "serve": "python3 -m http.server 8080",
    "preview": "npm run build && npm run serve",
    "review": "node tools/review.mjs",
    "notes": "node tools/review.mjs",
    "newsletter": "node tools/newsletter.mjs",
    "deploy": "firebase deploy --only hosting",
    "publish": "npm run build && npm run validate && firebase deploy --only hosting"
  },
  "engines": { "node": ">=18" }
}
